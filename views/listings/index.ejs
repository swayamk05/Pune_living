<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wanderlust - Listings</title>
    <!-- Bootstrap CSS for styling -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap" rel="stylesheet">
</head>
<body>
    <%- include('../includes/navbar.ejs') %>

    <main class="container my-5">
        <%- include('../includes/flash.ejs') %>

        <!-- The heading is now dynamic. It changes based on whether a search was made. -->
        <h1 class="mb-4"><%= typeof searchQuery !== 'undefined' && searchQuery ? `Results for "${searchQuery}"` : "All Listings" %></h1>

        <div class="row row-cols-1 row-cols-md-4 g-4">
            <!-- Check if there are any listings to display -->
            <% if(allListings.length > 0) { %>
                <% for (let listing of allListings) { %>
                    <div class="col">
                        <a href="/listings/<%= listing._id %>" class="listing-card text-decoration-none">
                            <div class="card h-100 border-0 shadow-sm">
                                <% if (listing.images && listing.images.length > 0) { %>
                                    <img src="<%= listing.images[0].url.replace('/upload', '/upload/w_300,h_200,c_fill') %>" class="card-img-top" alt="<%= listing.title %>">
                                <% } else { %>
                                    <img src="https://placehold.co/300x200/282828/FFFFFF?text=No+Image" class="card-img-top" alt="Placeholder for <%= listing.title %>">
                                <% } %>
                                <div class="card-body">
                                    <h5 class="card-title fw-bold"><%= listing.title %></h5>
                                    <p class="card-text">₹<%= listing.price.toLocaleString("en-IN") %> / night</p>
                                </div>
                            </div>
                        </a>
                    </div>
                <% } %>
            <% } else { %>
                <!-- This message is shown if no listings match the search -->
                <div class="col-12">
                    <div class="alert alert-warning" role="alert">
                        <h4>No Listings Found</h4>
                        <p>We couldn't find any listings matching your search for "<%= searchQuery %>". Please try a different location or browse all our listings.</p>
                        <a href="/listings" class="btn btn-dark">View All Listings</a>
                    </div>
                </div>
            <% } %>
        </div>
    </main>

    <%- include('../includes/footer.ejs') %>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

